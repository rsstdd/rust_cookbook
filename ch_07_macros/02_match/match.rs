//! Implementing common macros in rust

fn main() {
    let v = vec![1, 2, 3, 4, 5];
    print!("Vector :- {:?}", v);

    assert!(true);
    assert_eq!(5, 3 + 2);

    // assert!(false);

    // panic!("oh, no")
}
